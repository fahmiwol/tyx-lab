# recipes/ — Combinations

An atom does one thing. A recipe shows how to **stitch several atoms** into a working
use case: which atoms, in what order, to produce what. See `CLAUDE.md` for the format.
