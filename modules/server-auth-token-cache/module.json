{
  "id": "server-auth-token-cache",
  "name": "Server Auth Token Cache",
  "version": "1.0.0",
  "category": "infra",
  "description": "Self-healing JWT token cache for backend services. Auto-refresh when expiry approaches (60s buffer). Graceful fallback to static token. Extracts exp claim from JWT payload.",
  "input": [
    { "name": "email", "type": "string", "description": "Login credentials (env var)" },
    { "name": "password", "type": "string", "description": "Password (env var)" },
    { "name": "staticToken", "type": "string", "description": "Fallback token if login fails" }
  ],
  "output": [
    { "name": "token", "type": "string | null", "description": "Valid JWT to use in API requests" }
  ],
  "tags": ["auth", "jwt", "token", "cache", "self-healing", "backend"],
  "compatible_with": ["nodejs", "typescript", "express", "fastapi", "django"],
  "used_in_recipes": [],
  "status": "stable",
  "author": "Tiranyx",
  "license": "MIT"
}
