{
  "name": "cursor-003-mighan-photo",
  "version": "0.1.0",
  "private": true,
  "description": "by Cursor — Photo editor + Sharp API",
  "scripts": {
    "server": "node server/index.js",
    "client": "npm run dev --prefix client",
    "start": "node server/index.js",
    "dev": "concurrently -n api,ui -c cyan,magenta \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "multer": "^1.4.5-lts.1",
    "sharp": "^0.33.5"
  }
}
